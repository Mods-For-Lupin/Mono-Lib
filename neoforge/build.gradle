plugins {
    id 'multiloader-loader'
    id 'net.neoforged.moddev'
}

dependencies {
    implementation 'maven.modrinth:dis-enchanting-table:5.1.1-neoforge,1.21.10'
    implementation 'maven.modrinth:auto-message:3.0.0-neoforge,1.21.10'
    implementation 'maven.modrinth:longer-following-time:1.1.0-neoforge,1.21.10'
    implementation 'maven.modrinth:more-beautiful-torches:3.0.0-neoforge,1.21.10'

    // TODO lower MonoLib version to 3.X.0?
    // built against an invalid version number of MonoLib (does not allow 4+)
    // implementation 'maven.modrinth:golden-foods:2.4.0-neoforge,1.21.10'

    // modrinth project still under review
    // implementation 'maven.modrinth:unlocked-typing:1.0.0-neoforge,1.21.10'
}

neoForge {
    version = neoforge_version
    // Automatically enable neoforge AccessTransformers if the file exists
    def at = project(':common').file('src/main/resources/META-INF/accesstransformer.cfg')
    if (at.exists()) {
        accessTransformers.from(at.absolutePath)
    }
    parchment {
        minecraftVersion = parchment_minecraft
        mappingsVersion = parchment_version
    }
    runs {
        configureEach {
            systemProperty('neoforge.enabledGameTestNamespaces', mod_id)
            ideName = "NeoForge ${it.name.capitalize()} (${project.path})" // Unify the run config names with fabric
        }
        client {
            client()
        }
        data {
            clientData()

            // DataGen can be run by - "./gradlew :neoforge:runData" in Terminal.
            // Specify the modid for data generation, where to output the resulting resource, and where to look for existing resources.
            programArguments.addAll '--mod', project.mod_id, '--all', '--output', file('src/generated/resources/').getAbsolutePath(), '--existing', file('src/main/resources/').getAbsolutePath()
        }
        server {
            server()
        }
    }
    mods {
        "${mod_id}" {
            sourceSet sourceSets.main
        }
    }
}

sourceSets.main.resources { srcDir 'src/generated/resources' }